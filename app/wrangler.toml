name = "haveadream"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

# KV namespace: Storage & databases → Workers KV → had-newsletter → copy Namespace ID
# Paste the id below, commit, push — Pages reads bindings from this file.
[[kv_namespaces]]
binding = "NEWSLETTER_KV"
id = "72057bf882d64095ae961213eb9dd479"
